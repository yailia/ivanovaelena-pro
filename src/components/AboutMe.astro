---
---

<section class="about-me-section">
	<div class="about-me-background">
		<div class="about-me-gradient"></div>
		<div class="about-me-gradient-circles">
			<div class="about-me-circle about-me-circle-1"></div>
			<div class="about-me-circle about-me-circle-2"></div>
		</div>
	</div>
	<div class="container">
		<div class="about-me-content">
			<div class="about-me-image">
				<img 
					src="/images/about-photo.jpg" 
					alt="Психолог Елена Иванова" 
					class="about-me-photo"
					loading="lazy"
				/>
			</div>
			<div class="about-me-text">
				<h2 class="section-title">Обо мне</h2>
				<div class="about-me-info">
					<h3 class="about-me-name">Елена Иванова</h3>
					<p class="about-me-qualification">Психолог, специалист по работе с тревогой и стрессом</p>
					<div class="about-me-details">
						<p class="about-me-experience">Опыт работы с тревогой, выгоранием и повышенной нагрузкой.</p>
						<p class="about-me-focus">Фокус в работе: тревога, выгорание, утрата фокуса, повышенная нагрузка.</p>
					</div>
					<div class="about-me-links">
						<a href="/blog" class="about-link">Статьи</a>
						<a href="/video" class="about-link">Эксперт в СМИ</a>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.about-me-section {
		position: relative;
		padding: var(--spacing-section) 0;
		overflow: hidden;
	}

	.about-me-background {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		z-index: 0;
	}

	.about-me-gradient {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(180deg, #ffffff 0%, #f0fdf9 50%, #ffffff 100%);
	}

	.about-me-gradient-circles {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		pointer-events: none;
	}

	.about-me-circle {
		position: absolute;
		border-radius: 50%;
		filter: blur(100px);
		opacity: 0.4;
	}

	.about-me-circle-1 {
		width: 500px;
		height: 500px;
		background: linear-gradient(135deg, #a8e6cf 0%, #6FD8C5 100%);
		top: 50%;
		left: -150px;
		transform: translateY(-50%);
	}

	.about-me-circle-2 {
		width: 400px;
		height: 400px;
		background: linear-gradient(135deg, #a8c9ff 0%, #3A8DFF 100%);
		top: 30%;
		right: -100px;
	}

	.about-me-content {
		position: relative;
		z-index: 1;
		display: grid;
		grid-template-columns: 1fr 1.2fr;
		gap: clamp(3rem, 6vw, 5rem);
		align-items: center;
		max-width: 1100px;
		margin: 0 auto;
		border: 1px solid rgba(111, 216, 197, 0.4);
		border-radius: 1.5rem;
		padding: var(--spacing-card);
		box-shadow: 0 8px 32px rgba(111, 216, 197, 0.15);
		background: #ffffff;
	}

	.about-me-image {
		display: flex;
		justify-content: center;
		align-items: center;
		border: 1px solid var(--color-primary);
		border-radius: 1.5rem;
	}

	.about-me-photo {
		width: 100%;
		max-width: 450px;
		height: auto;
		object-fit: cover;
		aspect-ratio: 3 / 4;
		border-radius: 1.5rem;
	}

	.about-me-text {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}

	.section-title {
		font-size: var(--font-size-h2);
		font-weight: 500;
		color: var(--color-text-primary);
		margin: 0;
	}

	.about-me-name {
		font-size: var(--font-size-h3);
		font-weight: 500;
		color: var(--color-text-primary);
		margin: 0 0 0.5rem 0;
	}

	.about-me-qualification {
		font-size: var(--font-size-body);
		color: var(--color-text-secondary);
		margin: 0 0 1.5rem 0;
	}

	.about-me-details {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.about-me-experience,
	.about-me-focus {
		font-size: var(--font-size-body);
		color: var(--color-text-primary);
		line-height: 1.6;
		margin: 0;
	}

	.about-me-links {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
		margin-top: 1.5rem;
	}

	.about-link {
		display: inline-flex;
		align-items: center;
		padding: 0.5rem 1rem;
		background: rgba(111, 216, 197, 0.15);
		border: 1px solid var(--color-primary);
		border-radius: var(--radius-button);
		color: var(--color-text-primary);
		font-size: var(--font-size-caption);
		text-decoration: none;
		transition: all 0.2s ease;
	}

	.about-link:hover {
		background: var(--color-primary);
		color: white;
	}

	@media (max-width: 768px) {
		.about-me-content {
			grid-template-columns: 1fr;
		}

		.about-me-image {
			order: -1;
		}

		.about-me-photo {
			max-width: 350px;
		}

		.about-me-circle-1 {
			width: 300px;
			height: 300px;
			left: -100px;
		}

		.about-me-circle-2 {
			width: 250px;
			height: 250px;
			right: -80px;
		}
	}
</style>
