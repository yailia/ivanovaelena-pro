---

---

<section class="final-cta-section">
  <div class="container">
    <div class="final-cta-content">
      <h2 class="final-cta-title">
        Если Вы чувствуете, что тревога лишает сил, запишитесь на первую встречу
      </h2>
      <a href="/contacts" class="btn btn-primary btn-large"
        >Записаться на консультацию</a
      >
    </div>
  </div>
</section>

<style>
  .final-cta-section {
    padding: var(--spacing-section) 0;
    background-color: white;
  }

  .final-cta-content {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .final-cta-title {
    font-size: var(--font-size-h2);
    font-weight: 500;
    color: var(--color-text-primary);
    line-height: 1.3;
    margin: 0;
  }

  .btn-large {
    height: 3.5rem;
    font-size: 1.125rem;
    padding: 0 2.5rem;
    align-self: center;
  }

  .btn-primary {
    background-color: var(--color-accent);
    color: white;
  }

  .btn-primary:hover {
    background-color: var(--color-accent-alt);
  }

  @media (max-width: 768px) {
    .final-cta-title {
      font-size: var(--font-size-h3);
    }
  }
</style>
